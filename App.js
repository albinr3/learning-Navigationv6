
import React from 'react';

import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';
import Navigation from './Navigation';


const App= () => {
  

  return (
   <>
    <Navigation/>
   </>
  );
};

const styles = StyleSheet.create({
  
});

export default App;
